# Optional: when set to a valid Calendly booking URL, the footer booking panel
# switches from the email/LinkedIn fallback to an embedded live scheduler.
PUBLIC_CALENDLY_URL=https://calendly.com/your-handle/intro-call
